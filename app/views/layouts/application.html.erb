<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Inception</title>
    <link rel="stylesheet" href="https://use.typekit.net/wtt0vuo.css">
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->

  </head>
  <body>
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <p class="alert"><%= alert %></p>

     <% if current_page?("http://localhost:3000/dreams/new") || current_page?("http://localhost:3000/escapists/sign_in") || current_page?("https://inceptionapp.herokuapp.com/dreams/new") || current_page?("https://inceptionapp.herokuapp.com/escapists/sign_in")%>
      <%= render "shared/footer" %>
      <% end %>
    <%= yield %>

    <% unless current_page?("http://localhost:3000/") || current_page?("https://inceptionapp.herokuapp.com/") || current_page?("http://localhost:3000/dreams/new") || current_page?("http://localhost:3000/escapists/sign_in") || current_page?("https://inceptionapp.herokuapp.com/escapists/sign_in") || current_page?("https://inceptionapp.herokuapp.com/dreams/new")%>
      <%= render "shared/navbar" %>
    <% end %>

    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>

  </body>
</html>
